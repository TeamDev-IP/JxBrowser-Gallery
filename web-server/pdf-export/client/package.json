{
  "name": "charts",
  "version": "1.0.0",
  "description": "The server-side PDF rendering application.",
  "scripts": {
    "build": "vite build --config vite.config.js"
  },
  "dependencies": {
    "gridjs": "^6.2.0"
  },
  "devDependencies": {
    "vite": "^5.1.2"
  }
}
