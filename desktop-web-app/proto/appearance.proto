syntax = "proto3";

package com.teamdev.jxbrowser.preferences;

message Appearance {
  FontSize font_size = 1;
  Theme theme = 2;
}

enum FontSize {
  SMALL = 0;
  DEFAULT = 1;
  LARGE = 2;
}

enum Theme {
  LIGHT = 0;
  DARK = 1;
  SYSTEM = 2;
}
